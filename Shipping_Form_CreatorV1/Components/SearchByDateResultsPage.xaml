<Page
    x:Class="Shipping_Form_CreatorV1.Components.SearchByDateResultsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Notification="clr-namespace:Syncfusion.Windows.Controls.Notification;assembly=Syncfusion.SfBusyIndicator.WPF"
    xmlns:local="clr-namespace:Shipping_Form_CreatorV1.Components"
    xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
    xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
    Title="Search Results"
    syncfusionskin:SfSkinManager.Theme="{syncfusion:SkinManager ThemeName=Office2019ColorfulOffice2019Colorful}">
    <Page.Resources>
        <syncfusion:SortDirectionToVisibilityConverter x:Key="sortDirectionToVisibilityConverter" />
        <syncfusion:SortDirectionToWidthConverter x:Key="sortDirectionToWidthConverter" />
        <Style x:Key="SfDataGridHeaderStyle1" TargetType="{x:Type syncfusion:GridHeaderCellControl}">
            <Setter Property="Background" Value="#e41f28" />
            <Setter Property="BorderBrush" Value="Gray" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="5,3,5,3" />
            <Setter Property="Foreground" Value="#FFF" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="syncfusion:VisualContainer.WantsMouseInput" Value="True" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type syncfusion:GridHeaderCellControl}">
                        <Grid>
                            <Border
                                x:Name="PART_FrozenCellBorder"
                                Background="Transparent"
                                BorderBrush="{TemplateBinding BorderBrush}" />
                            <Border
                                x:Name="PART_FooterCellBorder"
                                Background="Transparent"
                                BorderBrush="{TemplateBinding BorderBrush}" />
                            <Border
                                x:Name="PART_HeaderCellBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                <Grid Margin="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Focusable="False" />
                                    <Border x:Name="PART_FilterPopUpPresenter" Grid.Column="0" />
                                    <Grid
                                        x:Name="PART_SortButtonPresenter"
                                        Grid.Column="1"
                                        SnapsToDevicePixels="True">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="0" MinWidth="{Binding SortDirection, Converter={StaticResource sortDirectionToWidthConverter}, Mode=OneWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Path
                                            Width="8.938"
                                            Height="8.138"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Data="F1 M 753.644,-13.0589 L 753.736,-12.9639 753.557,-12.7816 732.137,8.63641 732.137,29.7119 756.445,5.40851 764.094,-2.24384 764.275,-2.42352 771.834,5.1286 796.137,29.4372 796.137,8.36163 774.722,-13.0589 764.181,-23.5967 753.644,-13.0589 Z"
                                            Fill="Gray"
                                            SnapsToDevicePixels="True"
                                            Stretch="Fill"
                                            Visibility="{Binding SortDirection, ConverterParameter=Ascending, Converter={StaticResource sortDirectionToVisibilityConverter}, RelativeSource={RelativeSource Mode=TemplatedParent}}">
                                            <Path.RenderTransform>
                                                <TransformGroup>
                                                    <RotateTransform Angle="0" />
                                                    <ScaleTransform ScaleX="1" ScaleY="1" />
                                                </TransformGroup>
                                            </Path.RenderTransform>
                                        </Path>
                                        <Path
                                            Grid.Column="0"
                                            Width="8.938"
                                            Height="8.138"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Data="F1 M 181.297,177.841 L 181.205,177.746 181.385,177.563 202.804,156.146 202.804,135.07 178.497,159.373 170.847,167.026 170.666,167.205 163.107,159.653 138.804,135.345 138.804,156.42 160.219,177.841 170.76,188.379 181.297,177.841 Z"
                                            Fill="Gray"
                                            SnapsToDevicePixels="True"
                                            Stretch="Fill"
                                            Visibility="{Binding SortDirection, ConverterParameter=Decending, Converter={StaticResource sortDirectionToVisibilityConverter}, RelativeSource={RelativeSource Mode=TemplatedParent}}">
                                            <Path.RenderTransform>
                                                <TransformGroup>
                                                    <RotateTransform Angle="0" />
                                                    <ScaleTransform ScaleX="1" ScaleY="1" />
                                                </TransformGroup>
                                            </Path.RenderTransform>
                                        </Path>
                                        <TextBlock
                                            Grid.Column="1"
                                            Margin="0,-4,0,0"
                                            VerticalAlignment="Center"
                                            Foreground="{TemplateBinding Foreground}"
                                            SnapsToDevicePixels="True"
                                            Text="{TemplateBinding SortNumber}"
                                            TextBlock.FontSize="10"
                                            Visibility="{TemplateBinding SortNumberVisibility}" />
                                    </Grid>
                                    <syncfusion:FilterToggleButton
                                        x:Name="PART_FilterToggleButton"
                                        Grid.Column="2"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        SnapsToDevicePixels="True"
                                        Visibility="{TemplateBinding FilterIconVisiblity}" />
                                </Grid>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SfDataGridRowStyle1" TargetType="{x:Type syncfusion:VirtualizingCellsControl}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Gray" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type syncfusion:VirtualizingCellsControl}">
                        <Grid>
                            <Border
                                x:Name="PART_RowBorder"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" />
                            <Rectangle Clip="{TemplateBinding RowBackgroundClip}" Fill="{TemplateBinding Background}" />
                            <Border
                                Background="{TemplateBinding RowSelectionBrush}"
                                Clip="{TemplateBinding SelectionBorderClipRect}"
                                Visibility="{TemplateBinding SelectionBorderVisiblity}" />
                            <Border
                                Background="{TemplateBinding RowHoverBackgroundBrush}"
                                BorderBrush="{TemplateBinding RowHoverBackgroundBrush}"
                                BorderThickness="{TemplateBinding RowHighlightBorderThickness}"
                                Clip="{TemplateBinding HighlightBorderClipRect}"
                                SnapsToDevicePixels="True"
                                Visibility="{TemplateBinding HighlightSelectionBorderVisiblity}" />
                            <Rectangle
                                x:Name="PART_CurrentFocusRow"
                                Margin="{TemplateBinding CurrentFocusBorderMargin}"
                                Stroke="#666666"
                                StrokeDashArray="2,2"
                                StrokeThickness="1"
                                Visibility="{TemplateBinding CurrentFocusRowVisibility}" />
                            <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter />
                            </Border>
                            <Border
                                x:Name="PART_DragLineIndicator"
                                BorderBrush="#2B579A"
                                BorderThickness="0,0,0,2"
                                Visibility="Collapsed" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="alternatingRowStyle" TargetType="syncfusion:VirtualizingCellsControl">
            <Setter Property="Background" Value="#fff" />
        </Style>
        <Style x:Key="tableSummaryRowStyle" TargetType="syncfusion:TableSummaryRowControl">
            <Setter Property="Background" Value="Black" />
            <Setter Property="Foreground" Value="White" />
        </Style>
        <Style x:Key="RowStyle" TargetType="syncfusion:VirtualizingCellsControl">
            <Setter Property="Background" Value="#e8e9ea" />
        </Style>
        <Style x:Key="SfDataPagerNumericButtonStyle1" TargetType="{x:Type syncfusion:NumericButton}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#e41f28" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Height" Value="40" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Focusable" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type syncfusion:NumericButton}">
                        <Grid>
                            <Rectangle
                                x:Name="PART_CurrentPageIndicator"
                                Width="25"
                                Height="25"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Fill="{TemplateBinding HighlightingThemeBrush}"
                                RadiusX="12.5"
                                RadiusY="12.5" />
                            <Border
                                x:Name="PART_RootBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter
                                    x:Name="PART_ContentPresenter"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>


    <!--  Hook command handlers on the page  -->
    <Page.CommandBindings>
        <CommandBinding Command="{x:Static local:SearchByDateResultsPage.OpenPackingListCommand}" Executed="OpenPackingList_Executed" />
        <CommandBinding Command="{x:Static local:SearchByDateResultsPage.OpenBolCommand}" Executed="OpenBol_Executed" />
        <CommandBinding Command="{x:Static local:SearchByDateResultsPage.ProcessAllDocumentsCommand}" Executed="ProcessAllDocuments_Executed" />
    </Page.CommandBindings>

    <Notification:SfBusyIndicator
        AnimationType="Gear"
        Foreground="#e41f28"
        IsBusy="{Binding IsBusy}"
        ViewboxHeight="200"
        ViewboxWidth="200">
        <Grid HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel
                    Grid.Column="0"
                    Margin="10,0,0,0"
                    Orientation="Horizontal">
                    <TextBlock
                        FontSize="25"
                        FontWeight="Bold"
                        Foreground="#000"
                        Text="ORDERS SHIPPED " />
                    <TextBlock
                        FontSize="25"
                        FontWeight="Bold"
                        Text="{Binding SearchDateString}" />
                </StackPanel>


                <!--<Button
                    Grid.Column="1"
                    Margin="0,0,10,0"
                    Padding="10,5"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Background="#e31f26"
                    Command="{x:Static local:SearchByDateResultsPage.ProcessAllDocumentsCommand}"
                    Content="Process All Packing Lists"
                    Cursor="Hand"
                    Foreground="#FFF" />-->
            </Grid>
            <syncfusion:SfDataGrid
                x:Name="searchResultsDataGrid"
                Grid.Row="1"
                Margin="10"
                AllowResizingColumns="True"
                AlternatingRowStyle="{StaticResource alternatingRowStyle}"
                AlternationCount="2"
                AutoGenerateColumns="False"
                Background="#fff"
                Foreground="#000"
                GridLinesVisibility="Both"
                HeaderStyle="{DynamicResource SfDataGridHeaderStyle1}"
                IsReadOnly="True"
                ItemsSource="{Binding ElementName=DataPager, Path=PagedSource}"
                MouseDoubleClick="SearchResultsDataGrid_OnMouseDoubleClick"
                RowHeight="30"
                RowStyle="{StaticResource RowStyle}"
                SelectedItem="{Binding SelectedReport, Mode=TwoWay}"
                TableSummaryRowStyle="{StaticResource tableSummaryRowStyle}">
                <syncfusion:SfDataGrid.Columns>
                    <syncfusion:GridTextColumn
                        Width="100"
                        HeaderText="Order #"
                        TextAlignment="Right"
                        ValueBinding="{Binding Header.OrderNumberWithSuffix}" />                    
                    
                    <syncfusion:GridTextColumn
                        Width="100"
                        HeaderText="Customer #"
                        TextAlignment="Right"
                        ValueBinding="{Binding Header.ShipToCustNumber}" />
                    <syncfusion:GridTextColumn
                        Width="300"
                        HeaderText="Ship To"
                        TextAlignment="Right"
                        ValueBinding="{Binding Header.ShipToName}" />
                    <syncfusion:GridTextColumn
                        Width="150"
                        HeaderText="City, State"
                        TextAlignment="Right"
                        ValueBinding="{Binding Header.ShipToCityStZip}" />
                    <syncfusion:GridTextColumn
                        Width="150"
                        HeaderText="Carrier"
                        TextAlignment="Right"
                        ValueBinding="{Binding Header.CarrierName}" />
                    <syncfusion:GridTemplateColumn
                        Width="220"
                        AllowFocus="False"
                        HeaderText="Actions">
                        <syncfusion:GridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                    <!--  Packing List Button  -->
                                    <Button
                                        Margin="2,0"
                                        Padding="6,2"
                                        Background="#e41f28"
                                        Command="{x:Static local:SearchByDateResultsPage.OpenPackingListCommand}"
                                        CommandParameter="{Binding}"
                                        Content="Packing List"
                                        Cursor="Hand"
                                        Foreground="#FFF" />

                                    <!--  Bill of Lading Button  -->
                                    <Button
                                        Margin="2,0"
                                        Padding="6,2"
                                        Background="#555555"
                                        Command="{x:Static local:SearchByDateResultsPage.OpenBolCommand}"
                                        CommandParameter="{Binding}"
                                        Content="Bill of Lading"
                                        Cursor="Hand"
                                        Foreground="#FFF" />
                                </StackPanel>
                            </DataTemplate>
                        </syncfusion:GridTemplateColumn.CellTemplate>
                    </syncfusion:GridTemplateColumn>
                </syncfusion:SfDataGrid.Columns>
                <syncfusion:SfDataGrid.TableSummaryRows>
                    <syncfusion:GridTableSummaryRow Title="Total Orders Shipped: {ShippedCount}" ShowSummaryInRow="True">
                        <syncfusion:GridSummaryRow.SummaryColumns>
                            <syncfusion:GridSummaryColumn
                                Name="ShippedCount"
                                Format="'{Count:d}'"
                                MappingName="Header.OrderNumber"
                                SummaryType="CountAggregate" />
                        </syncfusion:GridSummaryRow.SummaryColumns>
                    </syncfusion:GridTableSummaryRow>
                </syncfusion:SfDataGrid.TableSummaryRows>
                <syncfusion:SfDataGrid.RecordContextMenu>
                    <ContextMenu>
                        <MenuItem
                            Command="{x:Static local:SearchByDateResultsPage.OpenPackingListCommand}"
                            CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                            Header="Open Packing List" />
                        <MenuItem
                            Command="{x:Static local:SearchByDateResultsPage.OpenBolCommand}"
                            CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                            Header="Open Bill of Lading" />
                    </ContextMenu>
                </syncfusion:SfDataGrid.RecordContextMenu>
            </syncfusion:SfDataGrid>
            <syncfusion:SfDataPager
                x:Name="DataPager"
                Grid.Row="2"
                HorizontalAlignment="Center"
                NumericButtonStyle="{DynamicResource SfDataPagerNumericButtonStyle1}"
                PageSize="25"
                Source="{Binding SearchByDateResults}" />
        </Grid>
    </Notification:SfBusyIndicator>
</Page>