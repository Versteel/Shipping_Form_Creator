<Page x:Class="Shipping_Form_CreatorV1.Components.SearchByDateResultsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:Shipping_Form_CreatorV1.Components"
      Title="Search Results">

    <!-- Hook command handlers on the page -->
    <Page.CommandBindings>
        <CommandBinding Command="{x:Static local:SearchByDateResultsPage.OpenPackingListCommand}"
                        Executed="OpenPackingList_Executed"/>
        <CommandBinding Command="{x:Static local:SearchByDateResultsPage.OpenBolCommand}"
                        Executed="OpenBol_Executed"/>
    </Page.CommandBindings>

    <Grid>
        <DataGrid x:Name="searchResultsDataGrid"
                  ItemsSource="{Binding SearchByDateResults}"
                  SelectedItem="{Binding SelectedReport, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  MouseDoubleClick="SearchResultsDataGrid_OnMouseDoubleClick"
                  Margin="10">

            <!-- Row-level context menu -->
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <EventSetter Event="PreviewMouseRightButtonDown"
                                 Handler="DataGridRow_PreviewMouseRightButtonDown" />
                    <Setter Property="ContextMenu">
                        <Setter.Value>
                            <ContextMenu>
                                <MenuItem Header="Open Packing List"
                                          Command="{x:Static local:SearchByDateResultsPage.OpenPackingListCommand}"
                                          CommandParameter="{Binding PlacementTarget.DataContext,
                                            RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                <MenuItem Header="Open Bill of Lading"
                                          Command="{x:Static local:SearchByDateResultsPage.OpenBolCommand}"
                                          CommandParameter="{Binding PlacementTarget.DataContext,
                                            RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            </ContextMenu>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.RowStyle>

            <!-- Columns -->
            <DataGrid.Columns>
                <DataGridTextColumn Header="Order #"
                                    Binding="{Binding Header.OrderNumber}" Width="100"/>
                <DataGridTextColumn Header="Ship Date"
                                    Binding="{Binding Header.ShipDate}" Width="110"/>
                <DataGridTextColumn Header="Ship To"
                                    Binding="{Binding Header.ShipToName}" Width="*"/>
                <DataGridTextColumn Header="City"
                                    Binding="{Binding Header.ShipToCity}" Width="150"/>
                <DataGridTextColumn Header="PO #"
                                    Binding="{Binding Header.CustomerPONumber}" Width="120"/>
                <DataGridTextColumn Header="Carrier"
                                    Binding="{Binding Header.CarrierName}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
